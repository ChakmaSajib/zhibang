import http from '';

// web socket

//  ws://localhost:8080/connectWebsocket/15 // here, user or hr id need to provide
// websocket.send(message) to change the message then, i will get the message
//need this kinds of data like this {"contentText": "message", "toUserId":"with whom i am going to talk"}
